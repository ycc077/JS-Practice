let A = 100;
let B = 200;
let C = 300;
let D = A + B + C;
let E = D * 0.8;

// Template String 字串模板
// 變數部分使用 ${} 進行標註
let sentence = `小明今天去買衣服總共花了${E}元，其中上衣的原價是${A}元，褲子原價是${B}元`;

console.log(sentence);
